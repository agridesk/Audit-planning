/** FILE: BatchPlanningAvailabilityIntervalTests.gs
 * BUILD: 2026-09-18_BATCH_PLANNING_AVAILABILITY_INTERVAL_R1
 * RUN: RUN_BATCH_PLANNING_AVAILABILITY_INTERVAL_REGRESSION
 */
function RUN_BATCH_PLANNING_AVAILABILITY_INTERVAL_REGRESSION(){var r=[],v=String(BatchPlanningConfirmation_validateFreshConstraints_);function t(n,o){r.push({name:n,ok:!!o});}t('canonicalRawAvailabilityUsed',v.indexOf('av.raw&&av.raw.days')>=0);t('explicitBlockTimesParsed',v.indexOf('BatchPlanningConfirmation_minutes_(blocks[z].start)')>=0&&v.indexOf('BatchPlanningConfirmation_minutes_(blocks[z].end)')>=0);t('hardIntervalsOnly',v.indexOf("it.kind!=='hard'&&it.hard!==true")>=0);t('sameAuditIgnored',v.indexOf("iid===String(req.auditId)")>=0);t('intervalOverlapFormula',v.indexOf('bs<ie&&is<be')>=0);t('specificOverlapError',v.indexOf('HARD_AVAILABILITY_TIME_OVERLAP')>=0);t('conflictMetadataReturned',v.indexOf('conflict:{start:it.startTime,end:it.endTime,auditId:iid')>=0);t('dayAvailabilityStillRequired',v.indexOf('HARD_AVAILABILITY_BLOCK')>=0);var passed=r.filter(function(x){return x.ok;}).length,out={ok:passed===r.length,build:'2026-09-18_BATCH_PLANNING_AVAILABILITY_INTERVAL_R1',total:r.length,passed:passed,failed:r.length-passed,results:r,meta:{nonDestructive:true,liveReadsPerformed:false,liveWritesPerformed:false,externalApiCallsPerformed:false,contract:'Atomic confirmation revalidates each requested explicit time block against canonical raw Availability hard intervals, not only day-level availability.'}};Logger.log(JSON.stringify(out,null,2));return out;}