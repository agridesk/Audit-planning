AUDIT MANAGEMENT SYSTEM — MASTER ROADMAP V2.6
Date: 2026-09-22
Last updated: 2026-09-23
Status: CONSOLIDATED VNEXT MASTER ROADMAP
Supersedes: Roadmap V2.5 as active planning baseline after acceptance
Operating principle: 3S + Value — Speed, Scalability, Stability + operational value

0. GOVERNANCE

Canonical technical chain:
Company -> Company Scope -> Audit Obligation -> Audit Visit <-> Availability

Canonical ownership:
- Model C: scope lifecycle, obligations, cycles, expiry, visit-obligation linkage.
- Audit planning: operational/compatibility projection; retained.
- Auditor Availability: operational Availability and derived capacity source.
- Config_Scopes: scope metadata / recurring lifecycle.
- Config_Scope_Dependencies: scope dependencies.
- canonical planning services: validation and writes.
- Notification Queue: communication delivery.
- Git repository: code SSoT.
- DEV GAS: canonical development runtime.

Hard rules:
- no second planning engine;
- no second Availability model;
- no duplicate validator policy;
- no second capacity truth;
- no silent dual truth;
- no direct bulk sheet edits;
- UI never becomes lifecycle owner;
- chats/Work are execution environments, not SSoT.

WIP rule:
- maximum one core technical stream + one product stream at a time.

READ-FIRST GOVERNANCE
For every new AMS execution chat or Work session that changes planning architecture or implementation:
1. read the current Master Roadmap;
2. read the Planning 2.0 Integrated Plan;
3. read the Planning 2.0 READ-FIRST governance document;
4. only then continue implementation.

This is mandatory to prevent local optimizations from drifting away from the accepted architecture.

1. CURRENT BASELINE — CLOSED / PROVEN

1.1 AMS-01 — Performance & Hot Paths
Status: substantially completed, but Planning 1.0 remains structurally too slow on several user-facing paths.
Proven foundation:
- bounded reads on key paths;
- reusable read models;
- cache/invalidation patterns;
- canonical commit instrumentation;
- hot-path diagnostics.

1.2 AMS-01.6 — Model C
Status: CLOSED ON DEV.
Accepted:
- Company -> Company Scope -> Audit Obligation -> Audit Visit;
- Recurring lifecycle from Config_Scopes;
- dependency ownership from Config_Scope_Dependencies;
- multiple child scopes;
- Must_Audit_Together / Share_Expiry;
- non-recurring Cycle_Key;
- scope-specific expiry;
- visit planning window = intersection of linked active obligations;
- invalid intersection hard-block;
- Audit planning remains operational projection.

1.3 Planning foundation
Implemented chain:
Planning Demand -> Concept Planning -> Workspace -> Preflight -> Canonical Commit -> Batch Commit -> Notification Queue.

Existing/proven capabilities:
- qualification;
- Availability;
- planning window;
- rotation;
- manager planning;
- auditor self-planning;
- two audits/day;
- multi-block planning;
- Modify / Cancel;
- Re-adjust foundation;
- batch isolation / outcomes;
- notification integration;
- optimistic revision / concurrency guard.

2. MASTER PRIORITY ORDER

A. Planning 2.0 — systematic replacement of slow Planning 1.0
B. Controlled Re-adjust / operational planned-audit modification
C. Annual workload, capacity and rotation control
D. Scope & certificate lifecycle evolution
E. Reporting & operational dashboards
F. Trip / workweek planning
G. Batch / bulk planning maturation
H. Company communication / proposal flows
I. Profiles, constraints and handover information
J. Data migration / legacy retirement / technical hardening
K. Long-term platform evolution

3. AMS-03 — PLANNING 2.0

Planning 2.0 is the umbrella replacement programme for the slow Planning 1.0 interaction model.

3.1 Problem
Planning 1.0 is too slow across:
- Manager Portal;
- Auditor Portal;
- Planning Toolkit;
- Availability Calendar;
- first load;
- month switch;
- audit selection;
- auditor selection;
- eligibility checks;
- rotation checks;
- Availability checks;
- Save;
- Modify / Re-adjust;
- refresh paths;
- overview reads.

This is not treated as isolated polishing anymore.

3.2 Shared architecture
Manager and Auditor use the same planning engine/read models/validators.
Differences are permissions and allowed actions, not duplicated logic.

MANDATORY MIGRATION RULE — PLANNING TOOLKIT
- The current Manager and Auditor Planning Toolkits are migration surfaces inside Planning 2.0, not separate end-state products or optimization programmes.
- Planning Toolkit performance measurements are diagnostic input for Planning 2.0; they must not trigger an isolated “Toolkit 1.5” architecture.
- Existing/proven Toolkit functionality is migrated into Concept Planning / Planning Workspace and the shared read-model / validation / preflight / canonical-commit chain.
- New planning capability must, by default, be implemented in the Planning 2.0 architecture rather than added structurally to the legacy Toolkit.
- Large Toolkit-specific rewrites are only justified when required as a temporary blocker fix or migration enabler.
- Manager and Auditor must converge on the same Planning 2.0 core; role differences are permission/action differences only.
- Availability Calendar, eligibility, qualification, rotation, planning windows, Re-adjust, Annual Planning, Batch Planning and Trip/Workweek Planning must converge on this same core.
- Legacy Toolkit paths may remain operational during migration, but are retired only after equivalent Planning 2.0 capability is proven.

READ-FIRST RULE FOR NEW AMS EXECUTION CHATS
Before changing Planning 2.0 architecture or implementation, re-read the current canonical master roadmap and the Planning 2.0 integrated governance documents from the repository.
At minimum:
1. docs/AMS_MASTER_ROADMAP_V2.6_2026-09-22.txt
2. docs/AMS_PLANNING_2_0_INTEGRATED_PLAN_2026-09-22.txt
3. docs/READ_FIRST_PLANNING_2_0_GOVERNANCE_2026-09-23.txt

If a proposed change conflicts with these documents, the roadmap/governance decision must be updated explicitly first; do not silently diverge.

Target chain:
Planning Demand
-> Concept Planning
-> Planning Workspace
-> Preflight
-> Canonical Commit
-> Batch Commit where relevant
-> Notification / Audit Trail

3.3 Availability Calendar Performance
Explicit P0/P1.
Measure and optimize:
- first load cold/warm;
- month switch;
- auditor switch;
- refresh after Save;
- refresh after Modify/Re-adjust.

Rules:
- month-bounded reads;
- no normal full-sheet scan;
- no N+1/day;
- no N+1/auditor;
- fast cache-hit path;
- deterministic invalidation;
- correctness guard after cache use.

3.4 Planning Toolkit / Workspace — Manager + Auditor
Measure:
- open;
- audit select;
- auditor select;
- decision-ready calendar;
- month switch;
- validator refresh;
- Save;
- Modify/Re-adjust;
- post-save close/refresh.

Direction:
- migrate proven Planning 1.0 functions into Planning 2.0 Workspace;
- avoid indefinite patching of old Toolkit;
- preserve canonical writers and validators.

3.5 Save & Close
- exactly one Save;
- deterministic close where launch context permits;
- safe fallback if browser close is technically impossible;
- no stale grid;
- no duplicate Save.

3.6 Workspace interaction
- scroll stability;
- drag/drop stability;
- modal lifecycle stability;
- context preservation;
- predictable refresh.

3.7 Performance targets
Initial:
- common cached/read-model interaction: target < 1 s;
- major decision-ready load: target approx. <= 2 s p95 where technically realistic;
- no repeated multi-second full-sheet scan on normal user action;
- performance regression evidence required.

Log:
- total ms;
- stage ms;
- rows read;
- cache/read-model path;
- fallback;
- build/version.

4. CONTROLLED RE-ADJUST / PLANNED-AUDIT MODIFICATION

Do not expose unrestricted generic Edit.

Initial supported changes:
- date;
- time;
- block distribution.

Lifecycle:
- Pending Approval -> preserve status;
- Approved -> preserve status;
- Accepted -> Approved + renewed auditor acceptance;
- Completed -> blocked;
- unsupported status -> blocked.

Initial governance:
- same auditor only;
- auditor replacement is separate high-impact action;
- Model C planning-window intersection authoritative;
- validate before Availability mutation;
- canonical modify service remains writer;
- notification and audit trail mandatory.

Later:
- controlled auditor replacement;
- execution location change;
- scope changes only under explicit lifecycle rules;
- bulk Re-adjust.

5. ANNUAL WORKLOAD, CAPACITY & ROTATION

5.1 Workload read model
Canonical input:
- Audit_Obligations;
- Audit_Visit_Obligations;
- operational Audit planning projection.

Show:
- obligations/audits to plan;
- formal hours to plan;
- hours planned;
- unallocated workload;
- planning window;
- lifecycle / expiry context;
- month;
- region;
- scope;
- assigned/preassigned auditor;
- status;
- Qualification;
- Rotation;
- Availability;
- constraints.

5.2 Capacity
Primary operational source:
Auditor Availability.

Capacity is derived, not separately maintained.

Inputs:
- standard working days;
- auditor blocked weekdays;
- explicit hard unavailability;
- explicit/default soft unavailability;
- occupied audit hours;
- Availability exceptions.

Output:
- gross standard capacity;
- hard unavailable hours;
- soft unavailable hours;
- occupied audit hours;
- available hours;
- overrideable soft hours;
- assigned workload;
- remaining headroom.

5.3 Planner annual view
Combine workload and capacity in one planning decision surface.
Requirements:
- distinguish obligations from unique physical visits;
- no double-count of bundled scopes;
- actual planned date for month aggregation;
- reconciliation between workload and capacity;
- drilldown by auditor/month/region/scope.

5.4 Rotation preassignment warning
- never silently preassign an auditor with rotation limitation;
- visible warning before assignment;
- soft warning/waiver unless governance later hard-blocks;
- show count/threshold context;
- canonical rotation service remains owner.

6. SCOPE & CERTIFICATE LIFECYCLE

6.1 Scope-specific expiry
Core Model C principle:
- each recurring scope/obligation owns its own expiry;
- a visit may combine multiple obligations;
- combined visit does not merge lifecycle truth;
- visit planning window is intersection;
- legacy single expiry projection may only be a compatibility view.

6.2 Recurring vs non-recurring
Recurring=YES:
- certificate lifecycle;
- expiry required;
- successor obligation generated according to lifecycle rules.

Recurring=NO:
- no invented certificate expiry;
- Cycle_Key can create annual obligations;
- planning windows still supported;
- no automatic certificate-style successor unless explicitly configured.

6.3 Dynamic scope UI
- remove fixed 8-scope assumptions;
- Config_Scopes drives active scope metadata;
- UI renders scopes dynamically;
- dependencies respected generically.

6.4 Scope changes
Need explicit rules for:
- add scope;
- deactivate scope;
- dependency-driven child scope;
- new cycle;
- existing planned visit linkage;
- certificate history;
- no destructive delete of historical truth.

6.5 Expiry/reporting projection
Reporting and UI must be able to show:
- expiry per scope;
- cycle per scope;
- combined visit;
- earliest relevant expiry where a compatibility summary is needed;
- no misleading single-expiry interpretation.

7. REPORTING & DECISION INFORMATION

7.1 Reporting read models
Read-only, reusable and canonical.
Feed:
- dashboards;
- annual planner;
- exports;
- Company Map;
- operational reports.

7.2 Core reporting
- workload by region/scope/auditor;
- planned vs to-plan hours;
- capacity;
- scope-specific expiry;
- planning-window exposure;
- rotation pressure;
- approval/acceptance ageing;
- reschedule frequency;
- trip efficiency;
- notification exceptions;
- completed / overdue / upcoming lifecycle.

7.3 Exports
Where operationally useful:
- Excel;
- PDF;
- management summaries.
Exports must consume read models, not create a second calculation layer.

8. MANAGER COCKPIT / ATTENTION CENTER

Show actionable exceptions:
- planning-window urgency;
- expiring scopes;
- missing successor obligations;
- Pending Approval ageing;
- Approved not Accepted;
- notification anomalies;
- unassigned workload;
- capacity overload;
- rotation waivers;
- conflicts;
- incomplete company/location data;
- stale concepts;
- lifecycle/projection drift.

Cockpit is action/read surface, not lifecycle owner.

9. AUDITOR DASHBOARD

Show:
- assigned audits;
- pending acceptance;
- self-planning opportunities;
- upcoming work/trips;
- Availability conflicts;
- planning requests;
- handover notes;
- required actions.

Uses same Planning 2.0 engine/validators.

10. TRIP & WORKWEEK PLANNING

Build on Concept Planning, not separate engine.

Flow:
1. choose period;
2. group/select workload by region;
3. choose feasible auditor;
4. select audits;
5. assemble concept trip/workweek;
6. sequence visits;
7. validate;
8. canonical commit.

Inputs:
- required visit locations;
- Companies.Locations_JSON;
- duration;
- planning windows;
- Availability;
- working-day limits;
- auditor base;
- company constraints.

Travel modes:
- car;
- train where relevant;
- flight where relevant.

Compare:
- door-to-door time;
- feasibility;
- cost when reliable;
- schedule impact;
- sustainability burden.

Flight:
- explicit environmental penalty/soft score;
- no automatic hard block.

Hotel:
- visible in map/trip context;
- possible day start/end point;
- advisory/manual;
- no autonomous booking initially.

11. BATCH / BULK PLANNING

11.1 Concept batch
- select multiple workload items;
- create concept batch;
- assign/edit;
- per-item validation;
- whole-batch preflight;
- explicit Apply.

11.2 Commit governance
- no silent partial commit;
- commit-time revalidation;
- idempotent batch ID;
- locking/concurrency;
- rollback/isolation;
- explicit outcome:
  committed / blocked / failed / already committed / skipped.

11.3 Later bulk actions
- bulk Re-adjust;
- move trip/week;
- controlled auditor replacement;
- always retain per-audit validation.

12. COMPANY COMMUNICATION / PROPOSALS

12.1 Company proposal
Concept may propose:
- date;
- time block.

Company may:
- confirm;
- decline;
- provide partial availability;
- suggest alternatives.

Rules:
- company never commits final planning;
- feedback returns to Workspace;
- validators rerun at commit;
- no duplicate truth in email/portal.

12.2 Operational communication
Potential:
- richer manager/auditor emails;
- ICS;
- map/GPS links;
- contact info;
- preparation reminders;
- SMS escalation where justified.

All delivery through Notification Queue.

13. PROFILES, CONSTRAINTS & HANDOVER

13.1 Auditor Planning Profile
Potential fields/behaviour:
- active/role;
- blocked weekdays;
- region preferences;
- planning constraints;
- base/location where needed;
- qualifications remain canonical elsewhere if already owned.

13.2 Company Planning Profile
- preferred audit months;
- location constraints;
- contact/planning instructions;
- recurring scheduling constraints.

13.3 Handover Notes
Two levels:
- Company Handover;
- Audit/Visit Handover.

Examples:
- access;
- parking;
- local contact instructions;
- site-specific operational issue;
- logistics.

Governance:
- structured;
- auditable;
- permission-aware;
- not lifecycle truth;
- not generic CRM.

14. DATA MIGRATION / LEGACY RETIREMENT

14.1 Projection hygiene
- Audit planning remains supported;
- gradually remove reads that incorrectly treat projection as canonical;
- keep compatibility until dependent UI/workflows are migrated.

14.2 Legacy cleanup
- obsolete diagnostics;
- duplicate helpers;
- old scope assumptions;
- old cache paths;
- abandoned Toolkit fragments;
- naming inconsistencies;
- historical typo fallbacks such as Audit_ObligATIONS where safely removable.

14.3 Migration evidence
Every retirement requires:
- replacement proven;
- regression tests;
- DEV evidence;
- rollback path.

15. TECHNICAL HARDENING

- remove duplicate global ownership/load-order risks;
- preserve deterministic cache invalidation;
- reduce full-sheet scans;
- target bounded/windowed reads;
- avoid N+1 patterns;
- retain write guards;
- maintain DEV/PROD discipline;
- keep canonical SSoT documentation synchronized.

16. PLANNING INTELLIGENCE / ADVISORY

Only after core Planning 2.0 is fast and stable.

Assignment advisory may consider:
- qualification;
- region;
- continuity;
- travel burden;
- rotation pressure;
- Availability fit;
- workload/capacity.

Output:
- candidates;
- reasons;
- hard blocks;
- warnings/waivers;
- policy/config version;
- generated timestamp.

Manager remains decision owner.

Trip sequencing advisory:
- time windows;
- travel;
- working day;
- location order;
- hotel/airport;
- constraints.

No heavy optimizer until simple advisory proves insufficient.

17. LONG-TERM PLATFORM DIRECTION

Only after demonstrated need:
- archive partitioning;
- paged history;
- async rebuild/workers;
- dedicated datastore/backend;
- API-first architecture;
- mobile-focused UX;
- migration beyond GAS.

18. EXECUTION ORDER FROM CURRENT STATE

Phase 1 — close current capacity correction
- rerun corrected Annual Availability capacity acceptance;
- verify blocked weekdays Mo/Tu;
- close annual capacity foundation.

Phase 2 — Planning 2.0 performance baseline
- Availability Calendar;
- Manager Toolkit;
- Auditor Toolkit;
- first load/month switch/select/save/modify;
- identify full scans/N+1/repeated validators;
- establish p50/p95.

Phase 3 — Planning 2.0 fast read layer
- month-bounded Availability;
- compact Workspace decision model;
- shared Manager/Auditor payload;
- deterministic cache/invalidation.

Phase 4 — integrated annual planner
- workload + capacity;
- rotation warning;
- month/region/scope drilldown;
- concept-planning launch.

Phase 5 — operational actions
- Re-adjust Workspace integration;
- Save/Close practical completion;
- refresh/close behaviour;
- lifecycle-safe Modify.

Phase 6 — lifecycle/reporting
- scope-specific expiry surfaces;
- dynamic scope UI;
- reporting read models;
- Manager Cockpit;
- Auditor Dashboard.

Phase 7 — Batch Planning maturation
- annual planner multi-select;
- concept batch;
- whole-batch preflight;
- canonical batch commit UX.

Phase 8 — Trip & Workweek Planning
- multi-location;
- route/map;
- travel mode comparison;
- sustainability penalty;
- hotel context.

Phase 9 — company communication / profiles
- proposal/confirmation;
- richer notifications;
- structured constraints/handover.

Phase 10 — legacy retirement / platform hardening
- remove replaced paths;
- consolidate remaining technical debt;
- reassess need for architecture beyond GAS.

19. DEFINITION OF DONE PER WORKSTREAM

Every activated workstream requires:
- explicit operational problem;
- canonical owner;
- no new dual truth;
- performance baseline where relevant;
- regression tests;
- rollback/recovery for writes;
- status effects defined;
- Availability effects defined;
- notification effects defined;
- audit trail for mutation;
- no hidden policy;
- DEV closure evidence.

20. MASTER ROADMAP GOVERNANCE

This V2.6 consolidates:
- Master Project Governance;
- Project SSoT Structure;
- Roadmap V2.3;
- Roadmap V2.4 implementation decisions;
- Roadmap V2.5 consolidated;
- AMS-01.6 Model C closure;
- Planning 2.0 integrated plan;
- parked planning/toolkit/maps/reporting items;
- current September 2026 execution state.

After acceptance:
- V2.6 is the active master roadmap;
- Planning 2.0 remains a subordinate integrated programme under V2.6;
- V2.5 becomes historical baseline;
- implementation remains tracked through AMS workstreams;
- repository docs remain roadmap SSoT.

21. CANONICAL CONSOLIDATION — 2026-09-25

21.1 One roadmap SSoT
- This file is the single current AMS Master Roadmap.
- Older roadmap versions are historical/superseded inputs only and must not be used as current truth.
- Planning 2.0 Integrated Plan and READ-FIRST governance remain subordinate execution/architecture documents; they do not form parallel roadmaps.
- Later accepted decisions are consolidated here before implementation when they change roadmap policy.

21.2 Planning 1.0 -> 2.0 feature-parity cutover gate
Planning 2.0 must preserve/re-expose proven operational 1.0 capability before the corresponding legacy surface is retired. This includes, where currently operational:
- Manager and Auditor planning;
- company creation and company-data maintenance;
- company scope add/change/deactivate management;
- preferred audit months / company planning profile data;
- auditor -> manager company-data proposals/review where applicable;
- Availability;
- qualification and auditor/company eligibility;
- planning windows;
- lifecycle/status actions;
- scope/certificate lifecycle and extension handling;
- completed/rejected/history access;
- notifications/audit trail;
- locations / Company Map and operational supporting tools.
Migration rule per capability: identify existing canonical owner -> reuse backend/SSoT -> expose in 2.0 -> regression/performance acceptance -> only then retire 1.0 path. Do not rebuild working business logic merely because the UI changes.

21.3 Dynamic scope capacity
- Config_Scopes remains canonical.
- Initial supported design boundary: 20 active scopes.
- Scope handling must be dynamic within that boundary; adding/activating a configured scope must not require scope-specific code changes.
- Remove fixed positional scope assumptions from UI, qualification, notifications, exports, calendar, persistence and tests.
- Company Scope / Audit Obligation remain lifecycle owners under Model C.

21.4 Rotation — canonical threshold policy
Config_Scopes owns the applicable rotation maximum per scope.
- current count <= configured maximum: allowed;
- configured maximum + 1: warning/exception path; planning is only permitted with explicit permission context and must remain visible/auditable;
- configured maximum + 2 or more: HARD BLOCK; planning is not possible.
No UI may silently weaken this policy. Canonical rotation validation remains the owner.

21.5 Self-planning and acceptance
- Manager-planned audit: Pending Planning -> Plan -> Approved; Auditor Accept -> Accepted.
- Auditor self-planning: Pending Planning -> Plan -> Pending Approval; Manager Approve -> Accepted directly. The auditor does not perform a redundant second Accept after submitting the self-planning.
- For a manager-planned Approved audit, Manager may Accept on behalf of the assigned Auditor when the Auditor has confirmed outside the system.
- Every Accepted transition records the actual actor. Manager-on-behalf acceptance additionally records the auditor on whose behalf acceptance occurred, source and reason/context. Acceptance provenance is auditable.

21.6 Controlled Re-adjust — materiality rule
Re-adjust remains an action/mode through Workspace -> Preflight -> canonical writer.
- Change of audit day/date: material -> return to Pending Planning and restart normal planning flow.
- Change of scope(s) affecting the current planned visit/audit: material -> return to Pending Planning and restart normal planning flow.
- Scope/certificate lifecycle changes that affect only future certification cycles/obligations and do not change the current planned visit are not a reason to reset the current audit status.
- Time change within the same audit day (for example 08:00-16:00 to 09:00-17:00): preserves the current lifecycle status, including Accepted, provided canonical Availability/conflict/planning-window validation passes. No renewed auditor acceptance is required solely for this same-day time change.
- When an Accepted audit keeps its status after such a manager-made same-day time change, the Auditor Portal must show a small, non-blocking warning/tooltip on that audit indicating that the manager changed the audit time. The signal remains visible until the auditor has seen/acknowledged the change; it is informational and does not introduce a new lifecycle status or acceptance step.
- Other non-material changes may preserve status when they do not change the audit day or scope set and pass the relevant canonical validators.
- Change of execution location while audit day/date, assigned auditor and scope set remain unchanged may preserve Approved/Accepted status, provided canonical Availability, travel/conflict and planning-validity checks pass. Execution-location change is not by itself a lifecycle reset trigger.
- Change of assigned auditor while date/day, scopes and planned time structure remain unchanged preserves the planning itself. The new auditor must explicitly accept the assignment before the audit can be Accepted; an already Accepted audit therefore returns to Approved for re-acceptance by the new auditor. Before reassignment is committed, canonical qualification, rotation, Availability, same-day capacity/overlap, travel/conflict and planning-validity checks must rerun for the replacement auditor; hard failures block reassignment.

- If a company/scope formal-hours change makes an already Approved/Accepted Visit under-planned, preserve the current lifecycle status. Surface a hard planning-validity warning/block until planned hours are corrected; do not silently reset lifecycle status. The audit cannot progress through planning-dependent downstream completion while this hard inconsistency remains.
- Completed remains immutable for operational Re-adjust.
This rule supersedes older generic wording that every time/hours/block change automatically resets lifecycle status.

21.7 3S performance — all user-facing mutation paths
Speed is a release criterion for every migrated/new 2.0 capability, not a later polishing phase.
Required hot mutations include at minimum:
- add/change/deactivate company audit scope and resulting hours recalculation;
- lifecycle/status transition;
- planning Save / Modify / Re-adjust / Cancel;
- extension apply/undo and resulting effective expiry/planning-window projection;
- company/planning-profile updates that affect planner decisions.
Targets:
- local UI response immediate where no server decision is required;
- common cached/read-model interaction < 1 s target;
- major decision-ready load approx. <= 2 s p95 where technically realistic;
- no normal mutation may trigger repeated multi-second full-sheet scans or broad rebuilds.
Required strategy: targeted/indexed reads and writes, compact read models, deterministic targeted invalidation, optimistic UI only where canonical response can safely reconcile, and no synchronous non-critical work that unnecessarily blocks the user response. Correctness/transactional invariants remain mandatory.
Performance acceptance evidence is required per migrated capability; functional parity alone is not sufficient.

21.8 Extension interaction
Extension apply/undo is a Model C obligation/cycle mutation, not Visit lifecycle truth. It must update effective expiry/planning-window projections through the canonical owner, invalidate only affected read models/caches, and return a refreshed decision result without a broad full-grid rebuild. Extension interaction is explicitly a 3S hot path.


21.9 Navigation and completion UX
- After a successful Planning Toolkit Save/Submit, the target Planning 2.0 flow closes the toolkit automatically and restores the originating operational context where browser context permits. No redundant post-save confirmation/Close step is part of the normal flow.
- After successful authentication, continue directly to the intended Manager/Auditor destination. No intermediate Open portal/Continue click in the normal flow; retain only a technical fallback when automatic navigation is blocked.
- Both requirements are part of 3S perceived-latency acceptance and must reuse canonical save/authentication outcomes rather than duplicate business or authentication logic.

21.10 1.0 -> 2.0 operational surface inventory and migration contract
The parity gate in 21.2 is executed as an operational surface migration, not as a rewrite. Proven 1.0 functions identified in the repository must remain available through 2.0 or an explicitly accepted replacement before the old surface is retired.

Manager-facing parity includes at minimum:
- operational open-audit grid and lifecycle actions;
- Planning Toolkit / Planning Workspace launch;
- Companies management, including create/edit and canonical Locations_JSON handling;
- Scope Manager: company scope add/change/deactivate, formal hours, certificate birthday/expiry and dependency rules;
- Rotation visibility/validation;
- Planning Overview / annual workload context;
- Company Map and auditor/workload context;
- Company update/proposal review;
- completed/history and rejected views;
- operational exports that remain in use.

Auditor-facing parity includes at minimum:
- assigned/open audit overview and self-planning;
- Planning Toolkit with auditor lock and Availability context;
- Accept/Cancel/Deny or their canonical successor actions according to 21.5;
- company-data adjustment/proposal flow;
- preferred audit months editing/proposal;
- own Availability management;
- completed/history access needed operationally.

Shared parity includes:
- company/location DTOs;
- Config_Scopes-driven scope metadata;
- qualification/eligibility;
- planning-window and expiry/extension context;
- notification queue/audit trail;
- identity/authorization and DEV/PROD separation.

For every surface, migration evidence must state: existing UI entry point, canonical backend/SSoT owner, 2.0 entry point, functional acceptance, 3S/performance acceptance and legacy-retirement status. Missing UI exposure is not permission to duplicate the backend.

21.11 Existing implementation evidence is reusable
Existing accepted components/tests are migration assets, including Model C Scope ownership, extension service, canonical planning modify/Re-adjust foundation, Planning Workspace, Availability, company update dialog/proposal flow, Manager Tools, maps and lifecycle services. Planning 2.0 must consume or adapt these owners. A new UI does not reset completed backend work.

21.12 Scope/cycle mutation status isolation
- Current Visit lifecycle status changes only when the mutation materially changes that current planned Visit.
- Adding/removing/changing a scope linked to the current planned Visit is material and follows 21.6.
- Company Scope / certificate changes that only affect future certification cycles or future obligations do not reset the current Visit status.
- Extension apply/undo is obligation/cycle-specific and does not by itself reset a current Visit status unless it materially changes that Visit's canonical planning validity; any resulting invalidity is handled explicitly by canonical validation, never by an implicit blanket status reset.
- Historical scope/certificate truth is never destructively deleted.


21.13 Company data and preferred-period mutation isolation
- Companies remains canonical company truth; Locations_JSON remains the sole canonical location owner.
- Auditor-originated company-data/preferred-period changes remain proposals until manager review/acceptance; they do not directly mutate Companies.
- Preferred audit months/period are planning guidance for future planning and do not by themselves alter the lifecycle status or committed Planning JSON of an already planned Visit.
- General company/contact/comment/constraint updates do not reset an active Visit merely because company master data changed. If a changed canonical field materially affects the current Visit (for example its selected execution location or another planning validator input), the current Visit is explicitly revalidated under 21.6 rather than blanket-reset.
- Accepted proposal application must use targeted cache/read-model invalidation and audit trail; no duplicate company truth and no broad rebuild.

21.14 Re-adjust confirmation and notification semantics
- A lifecycle-preserving Re-adjust must still create an audit-trail event containing actor, timestamp, changed fields and before/after values or equivalent deterministic change metadata.
- Material changes that require renewed approval/acceptance use the canonical lifecycle notification for the resulting state; informational changes that preserve status must not manufacture a lifecycle transition.
- Manager-made same-day time changes on Accepted audits use the informational auditor signal defined in 21.6. Similar informational signals may be used for other lifecycle-preserving changes only where operationally useful; they remain separate from status.
- Notification delivery failure never rolls back an otherwise valid canonical mutation; Notification Queue remains the delivery SSoT.


21.15 Company master-data parity and ownership
- Manager 2.0 must retain the existing Companies operational surface: list/search, create company, edit company, status, country/time zone, contacts, communication language, region, comments, planning preferences and Locations_JSON-backed locations.
- Manager company creation/editing writes through the existing canonical Companies backend; 2.0 must not introduce a second company store or duplicate write path.
- Auditor company adjustments remain proposal-only. Company_Update_Proposals is the staging/review path; manager review accepts/rejects and accepted changes are applied to Companies with audit trail.
- Preferred audit months and less-suitable weekdays/hours remain planning preferences/soft constraints, not hard planning blocks. Changes do not rewrite existing Planning JSON or lifecycle status by themselves.
- New companies must become available to Scope Manager / Model C ownership without manual cache repair; targeted invalidation/read-model refresh is part of acceptance.

21.16 Hard planning validity versus manager discretion
- Canonical hard planning failures remain non-overridable by ordinary Manager/Auditor UI actions: double booking/overlap, auditor unavailable, outside canonical planning window, invalid qualification/dependency, and other validator-defined hard failures.
- Manager discretion applies only to explicitly classified soft warnings/preferences. A UI acknowledgement may record the decision but may not convert a hard failure into a valid canonical plan.
- Weekend handling, travel burden, sustainability preference, company less-suitable days/hours and rotation pressure remain governed by their explicit rule classifications; rotation specifically follows 21.4.
- Planning 2.0 must render hard versus soft outcomes distinctly and reuse one canonical validator owner for Manager, Auditor, Re-adjust and later batch/bulk planning.
